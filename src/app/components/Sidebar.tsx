'use client';

export default function Sidebar() {
  return (
    <aside className="sidebar">
      <a href="#">Login</a>
      <a href="#">Cart</a>
      <a href="#">Orders</a>
      <a href="#">Profile</a>
    </aside>
  );
}